import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.reqstorunitmain',
  appName: 'reqs-to-run-it-main',
  webDir: 'dist'
};

export default config;
